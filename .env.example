# Optional. Without it the app runs the deterministic router: same governed
# SQL, same evidence pack, templated narrative instead of a written one.
ANTHROPIC_API_KEY=sk-ant-...

# Optional overrides
PI_MODEL=claude-sonnet-5
PI_MAX_AGENT_STEPS=8
